import random

# Generates and returns random float between 0.0 to 1.0
print(random.random())

# Generates and returns random integer between the given range ( randint(start, end) )
print(random.randint(1,6))
